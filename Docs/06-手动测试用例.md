# DataETL2 手动测试用例

**适用版本**: Phase 1 Day 1–3（登录 / 数据源管理 / 文件上传 / SFTP 浏览 / 字段映射编辑器）  
**测试环境**: 浏览器访问 http://localhost:8080  
**测试账号**: 用户名 `admin`，密码 `admin123`  
**更新日期**: 2026-05-05

---

## 变更日志

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-05-05 | v1.2 | 新增 TC-05 字段映射编辑器（6 个测试场景）；更新测试账号密码为 `admin123`；更新快速验证清单含 Day 3 步骤；更新适用版本至 Day 3 |
| 2026-05-04 | v1.1 | 初版：TC-01 至 TC-04，共 57 条用例；适用 Day 1–2 |

---

## 说明

| 符号 | 含义 |
|------|------|
| 👉 | 操作步骤 |
| ✅ | 预期结果（通过标准）|
| ❌ | 预期结果（应报错）|
| 📝 | 测试记录列（测试时在此打勾）|

每条用例执行后，在「结果」列填写 **通过 / 失败 / 跳过**。

---

## TC-01 登录与鉴权

### TC-01-1 正常登录

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 打开 http://localhost:8080 | ✅ 自动跳转到 `/login` 登录页 | 📝 |
| 2 | 查看登录页元素 | ✅ 显示"DataETL2"标题、用户名输入框、密码输入框、登录按钮 | 📝 |
| 3 | 输入用户名 `admin`，密码 `Admin123!`，点击登录 | ✅ 跳转到主页，左侧显示导航菜单 | 📝 |
| 4 | 查看右上角 | ✅ 显示当前用户名 `admin` | 📝 |
| 5 | 查看左侧导航菜单 | ✅ 包含「数据源管理」「文件上传」「SFTP 浏览」「字段映射」「执行历史」 | 📝 |

### TC-01-2 登录失败

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 输入用户名 `admin`，密码 `WrongPass`，点击登录 | ❌ 出现红色错误提示，停留在登录页 | 📝 |
| 2 | 输入不存在的用户名 `nobody`，任意密码 | ❌ 出现错误提示，不跳转 | 📝 |
| 3 | 两个输入框均为空，点击登录 | ❌ 表单验证提示，不发起请求 | 📝 |

### TC-01-3 退出登录

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 登录成功后，点击右上角用户头像 | ✅ 弹出下拉菜单，含「退出登录」 | 📝 |
| 2 | 点击「退出登录」 | ✅ 跳转到登录页 | 📝 |
| 3 | 在浏览器地址栏直接访问 http://localhost:8080/ | ✅ 被重定向到 `/login`，无法访问主页 | 📝 |

### TC-01-4 直接访问受保护页面

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 未登录状态下访问 http://localhost:8080/upload | ✅ 自动跳转到登录页 | 📝 |

---

## TC-02 数据源管理

> 导航：点击左侧「数据源管理」

### TC-02-1 页面加载

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击左侧「数据源管理」 | ✅ 页面显示数据源列表表格，右上角有「新建数据源」按钮 | 📝 |

### TC-02-2 创建文件上传类型数据源

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击「新建数据源」按钮 | ✅ 弹出新建对话框 | 📝 |
| 2 | 填写「数据源名称」：`月度基金净值` | — | 📝 |
| 3 | 填写「描述」：`每月手动上传` | — | 📝 |
| 4 | 选择「接入类型」：`文件上传（CSV / Excel）` | ✅ SFTP 相关字段不显示 | 📝 |
| 5 | 填写「Raw 层目标表名」：`raw_fund_nav` | — | 📝 |
| 6 | 点击「保存」 | ✅ 对话框关闭，列表新增一条记录，类型显示「文件上传」 | 📝 |

### TC-02-3 创建 SFTP 类型数据源

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击「新建数据源」 | ✅ 弹出对话框 | 📝 |
| 2 | 填写「数据源名称」：`本地SFTP测试` | — | 📝 |
| 3 | 选择「接入类型」：`SFTP 拉取` | ✅ 显示 SFTP 主机/端口/用户名/密码/远程目录/文件模式字段 | 📝 |
| 4 | 填写「Raw 层目标表名」：`raw_sftp_fund` | — | 📝 |
| 5 | 填写 SFTP 主机：`sftp` | — | 📝 |
| 6 | 填写端口：`22` | — | 📝 |
| 7 | 填写用户名：`etltest` | — | 📝 |
| 8 | 填写密码：`etltest123` | — | 📝 |
| 9 | 填写远程目录：`/upload` | — | 📝 |
| 10 | 填写文件模式：`*.csv` | — | 📝 |
| 11 | 点击「保存」 | ✅ 对话框关闭，列表新增 SFTP 类型记录，SFTP 主机列显示 `sftp` | 📝 |

### TC-02-4 创建数据源表单验证

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击「新建数据源」，不填任何内容，直接点「保存」 | ❌ 显示必填字段验证提示，不提交 | 📝 |
| 2 | 只填名称，不填 Raw 表名，点「保存」 | ❌ Raw 表名处显示验证提示 | 📝 |
| 3 | 选 SFTP 类型，不填 SFTP 主机，点「保存」 | ❌ 主机字段验证提示 | 📝 |

### TC-02-5 编辑数据源

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 对「月度基金净值」点击「编辑」按钮 | ✅ 弹出编辑对话框，已有字段值回填 | 📝 |
| 2 | 修改名称为「月度基金净值（已更新）」，点「保存」 | ✅ 列表中名称更新 | 📝 |
| 3 | 打开 SFTP 类型数据源的编辑，密码字段为空（不回显） | ✅ 密码框为空，留空则保持不变 | 📝 |

### TC-02-6 删除数据源

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 对任意一条记录点击「删除」 | ✅ 弹出确认提示「确认删除该数据源？」 | 📝 |
| 2 | 点击「取消」 | ✅ 取消，记录保留 | 📝 |
| 3 | 再次点击「删除」，点「删除」确认 | ✅ 记录从列表消失 | 📝 |

---

## TC-03 文件上传入库

> 导航：点击左侧「文件上传」  
> 前提：已存在至少一个「文件上传」类型的数据源（见 TC-02-2）

**准备测试文件**（在桌面或下载目录创建以下文件）：

`test_nav.csv` 内容：
```
date,fund,nav,currency
2026-01-01,FundA,102.50,USD
2026-01-02,FundB,98.30,USD
2026-01-03,FundC,115.00,HKD
```

### TC-03-1 文件预览（不入库）

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击左侧「文件上传」 | ✅ 页面分为「选择数据源」「选择文件」两个区域 | 📝 |
| 2 | 点击「选择文件」区域（或拖拽 `test_nav.csv`） | ✅ 文件名显示在上传区域 | 📝 |
| 3 | 点击「预览字段」按钮 | ✅ 下方出现预览卡片，显示列名（date/fund/nav/currency）、列类型、前 10 行数据 | 📝 |
| 4 | 检查预览行数 | ✅ 显示「共 3 行，显示前 10 行」 | 📝 |
| 5 | 此时不点「确认入库」，检查数据库是否有数据 | ✅ 预览不写库（可以通过后续 TC-03-3 入库后对比行数验证）| 📝 |

### TC-03-2 CSV 文件入库

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 在「选择目标数据源」下拉框选择「月度基金净值（已更新）」 | ✅ 显示对应的 Raw 表名 `raw_fund_nav` | 📝 |
| 2 | 上传 `test_nav.csv` | ✅ 文件名显示在区域内 | 📝 |
| 3 | 点击「确认入库」 | ✅ 出现入库结果卡片，显示 Run ID、入库行数=3、字段列表、前 5 行数据 | 📝 |
| 4 | 检查入库行数 | ✅ rows_ingested = 3 | 📝 |
| 5 | 检查 Run ID 格式 | ✅ 36 位 UUID（如 `a1b2c3d4-...`）| 📝 |
| 6 | 检查结果中是否含元数据列 | ✅ 样本数据中含 `_src_file`（文件名）、`_ingested_at`（时间）、`_run_id` | 📝 |

### TC-03-3 Excel 文件入库

**准备**：用 Excel 或 WPS 创建 `test_nav_feb.xlsx`，列名与 CSV 相同（date/fund/nav/currency），填入 3 行数据。

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 选择同一数据源「月度基金净值（已更新）」 | — | 📝 |
| 2 | 上传 `test_nav_feb.xlsx` | ✅ 文件名显示 | 📝 |
| 3 | 点击「确认入库」 | ✅ 入库成功，Run ID 与上次 CSV 不同 | 📝 |
| 4 | 两次入库后 raw_fund_nav 共有 6 行数据 | ✅ 累计追加，不覆盖 | 📝 |

### TC-03-4 错误场景

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 上传 `.txt` 或 `.json` 格式文件，点「确认入库」 | ❌ 提示「仅支持 CSV / Excel 文件」 | 📝 |
| 2 | 不选数据源，直接点「确认入库」 | ❌ 提示「请选择数据源」，按钮为灰色不可点 | 📝 |
| 3 | 不选文件，直接点「确认入库」 | ❌ 提示「请选择文件」 | 📝 |
| 4 | 上传列名与已有 Raw 表不匹配的 CSV（如只有 `a,b` 两列），点「确认入库」 | ❌ 提示「数据入库失败」（Schema 不兼容）| 📝 |

---

## TC-04 SFTP 文件浏览与拉取

> 导航：点击左侧「SFTP 浏览」  
> 前提：  
> ① 已创建 SFTP 类型数据源「本地SFTP测试」（见 TC-02-3）  
> ② `sftp-data/` 目录下已有 `sample_data.csv`（系统自动创建）

### TC-04-1 连接并浏览文件

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击左侧「SFTP 浏览」 | ✅ 显示数据源选择下拉框和「连接并浏览」按钮 | 📝 |
| 2 | 下拉选择「本地SFTP测试」 | ✅ 下方显示目标 Raw 表名 `raw_sftp_fund` | 📝 |
| 3 | 点击「连接并浏览」 | ✅ 下方出现文件列表，含 `sample_data.csv`，显示文件大小和修改时间 | 📝 |

### TC-04-2 拉取文件入库

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 对 `sample_data.csv` 点击「拉取入库」按钮 | ✅ 按钮显示 loading 状态 | 📝 |
| 2 | 拉取完成后 | ✅ 底部显示「最近入库结果」：Run ID、入库行数=3、字段列表 | 📝 |
| 3 | 页面顶部显示成功提示 | ✅ 绿色消息「已入库 3 行」 | 📝 |

### TC-04-3 新增 SFTP 文件后再次浏览

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 在 Finder 中打开 `/Users/Zhuanz/Claude/DataETL2/sftp-data/` | — | 📝 |
| 2 | 复制一个新的 CSV 文件（如 `test_nav.csv`）到该目录 | ✅ 文件出现在文件夹中 | 📝 |
| 3 | 回到浏览器，点击「连接并浏览」刷新 | ✅ 文件列表更新，出现新文件 | 📝 |
| 4 | 对新文件点「拉取入库」 | ✅ 如列名不同于已有表则报错；如列名相同则入库成功 | 📝 |

### TC-04-4 错误场景

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 创建一个主机填 `wrong-host`、密码随意的 SFTP 数据源，点「连接并浏览」 | ❌ 显示红色错误提示「SFTP 连接失败」 | 📝 |
| 2 | 不选数据源，直接点「连接并浏览」 | ❌ 提示「请先选择数据源」，按钮为灰色 | 📝 |

---

## TC-05 多用户角色测试

> 前提：使用 API 工具（或浏览器 Console）创建编辑者和查看者账号

### TC-05-1 注册测试账号

在浏览器控制台（F12 → Console）执行以下命令注册 editor 账号：

```javascript
fetch('/api/v1/auth/register', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({username:'editor1', email:'editor@test.com', password:'Test123!', role:'editor'})
}).then(r=>r.json()).then(console.log)
```

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 执行上方 JS，查看 Console 输出 | ✅ 返回用户信息，role=editor，不含密码字段 | 📝 |
| 2 | 退出 admin，用 `editor1` / `Test123!` 登录 | ✅ 成功登录，显示 editor1 | 📝 |

---

## TC-06 浏览器兼容性检查

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 在 Chrome 中完整走一遍 TC-02 + TC-03 | ✅ 正常 | 📝 |
| 2 | 在 Safari 中打开 http://localhost:8080 | ✅ 登录页正常渲染 | 📝 |
| 3 | 缩小浏览器窗口至 1024px 宽 | ✅ 布局不错位，侧边栏可见 | 📝 |

---

## TC-07 系统健壮性检查

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 登录后，在地址栏输入 http://localhost:8080/nonexistent | ✅ 不报白屏，显示「字段映射」或回退到主页（React Router 兜底）| 📝 |
| 2 | 在文件上传页，上传一个空 CSV（只有表头无数据行） | ✅ 显示 0 行预览，入库后提示 rows_ingested=0 | 📝 |
| 3 | 在数据源管理，快速连续点击「新建数据源」 | ✅ 只弹出一个对话框，不重复弹出 | 📝 |
| 4 | 上传一个超大 CSV（> 1MB）| ✅ 正常上传入库（Nginx 已配置 50MB 限制）| 📝 |

---

## 测试总结模板

```
测试人：_______________
测试日期：_______________
测试环境：http://localhost:8080

TC-01 登录与鉴权        通过 ___/7
TC-02 数据源管理        通过 ___/17
TC-03 文件上传入库      通过 ___/14
TC-04 SFTP 浏览与拉取   通过 ___/10
TC-05 多用户角色        通过 ___/2
TC-06 浏览器兼容性      通过 ___/3
TC-07 系统健壮性        通过 ___/4

总计：___/57

发现问题：
1.
2.
```

---

## TC-05 字段映射编辑器（Day 3）

> 前提：已有一个上传过文件的「文件上传」数据源（Raw 表中有数据）

### TC-05-1 自动加载字段列表

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击左侧导航「字段映射」 | ✅ 进入字段映射页，显示数据源下拉框 | 📝 |
| 2 | 在下拉框选择已有数据的数据源 | ✅ 表格自动展示字段列表，每行一个 Raw 字段 | 📝 |
| 3 | 确认表格列 | ✅ 包含：源字段、目标字段、目标类型、默认值、跳过 | 📝 |
| 4 | 确认目标字段默认值 | ✅ 与源字段相同（identity mapping） | 📝 |
| 5 | 确认 DWD 目标表名 | ✅ 自动建议为 `dwd_<raw表名去掉raw_前缀>` | 📝 |
| 6 | 确认元数据列不在列表中 | ✅ `_src_file`、`_ingested_at`、`_run_id` 不出现 | 📝 |

### TC-05-2 编辑映射规则

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 修改某行的「目标字段」名称 | ✅ 可直接在输入框编辑，实时更新 | 📝 |
| 2 | 修改某行的「目标类型」为 `float` | ✅ 下拉选择，Tag 颜色同步变为青色 | 📝 |
| 3 | 在「默认值」列输入 `0` | ✅ 可正常输入 | 📝 |
| 4 | 勾选某行的「跳过」复选框 | ✅ 该行整体变为灰色半透明 | 📝 |
| 5 | 确认被跳过行的输入框状态 | ✅ 目标字段/类型/默认值均变为禁用（不可编辑）| 📝 |
| 6 | 查看卡片标题统计 | ✅ 显示「N 个字段将写入 DWD」和「M 个跳过」| 📝 |

### TC-05-3 保存映射规则

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 填写 DWD 目标表名后点击「保存所有映射规则」 | ✅ 出现成功提示「字段映射已保存」| 📝 |
| 2 | 切换到其他页面再切换回字段映射 | ✅ 重新选择同一数据源后，保存的映射仍在 | 📝 |
| 3 | 修改部分规则后再次保存 | ✅ 新规则覆盖旧规则（非追加）| 📝 |
| 4 | 不填写 DWD 目标表名，点击保存 | ❌ 按钮禁用或出现警告，不发起请求 | 📝 |

### TC-05-4 下载 Excel 模板

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 点击「下载 Excel 模板」按钮 | ✅ 浏览器自动下载 `.xlsx` 文件 | 📝 |
| 2 | 打开下载的文件 | ✅ 第一行为表头：src_field / dst_field / dst_type / default_value / skip | 📝 |
| 3 | 查看后续数据行 | ✅ 每个 Raw 字段一行，dst_type 默认为 string，skip 默认为 FALSE | 📝 |
| 4 | 查看「说明」sheet | ✅ 列出所有 dst_type 可选值（string/integer/float/date/datetime/boolean）| 📝 |

### TC-05-5 从 Excel 导入映射

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 在模板文件中修改 dst_field、dst_type，保存 | - | 📝 |
| 2 | 点击「从 Excel 导入」，选择修改后的文件 | ✅ 表格内容更新为 Excel 中的规则 | 📝 |
| 3 | 确认 skip=TRUE 的行在 UI 中显示为跳过状态 | ✅ 该行灰色显示 | 📝 |
| 4 | 上传一个缺少 src_field 列的 Excel | ❌ 出现错误提示「Excel 缺少必要列」| 📝 |
| 5 | 上传后点击保存 | ✅ 导入的规则保存成功 | 📝 |

### TC-05-6 无数据时的错误处理

| 步骤 | 操作 | 预期结果 | 结果 |
|------|------|---------|------|
| 1 | 选择一个从未上传过文件的数据源 | ❌ 显示警告：「Raw 表不存在，请先上传或拉取文件」| 📝 |
| 2 | 不选数据源点击「从 Excel 导入」 | ❌ 按钮禁用或出现警告「请先选择数据源」| 📝 |

---

## 测试记录汇总（含 Day 3）

```
测试日期：_______________
测试人员：_______________
环境：http://localhost:8080

TC-01 登录与鉴权（4条）：___/4
TC-02 数据源管理（5条）：___/5
TC-03 文件上传（5条）：___/5
TC-04 SFTP 浏览（4条）：___/4
TC-05 字段映射编辑器（6条）：___/6

总计：___/24（Day 1–3）

发现问题：
1.
2.
```

---

## 附：快速验证清单（7分钟冒烟测试，含 Day 3）

1. ☐ 打开 http://localhost:8080 → 看到登录页
2. ☐ 用 `admin` / `admin123` 登录 → 进入主页
3. ☐ 新建「文件上传」数据源，Raw 表名 `raw_smoke_test`
4. ☐ 上传任意 CSV → 预览字段 → 确认入库 → 看到 Run ID 和行数
5. ☐ 新建 SFTP 数据源（主机 `sftp`，端口 22，用户 `etltest`，密码 `etltest123`，路径 `/upload`，表名 `raw_smoke_sftp`）
6. ☐ 进入「SFTP 浏览」→ 连接 → 看到 `sample_data.csv` → 拉取入库 → 看到成功提示
7. ☐ 进入「字段映射」→ 选择步骤 3 的数据源 → 表格显示字段列表
8. ☐ 修改一个字段的目标类型为 `float` → 保存 → 刷新后仍保留
9. ☐ 下载 Excel 模板 → 文件正常打开 → 有字段行和说明 sheet
10. ☐ 退出登录 → 跳转到登录页

全部打勾 = Day 1–3 核心功能正常。
